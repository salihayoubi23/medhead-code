spring:
  datasource:
    url: jdbc:h2:mem:medhead_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

# ✅ Valeurs "safe" pour les tests (évite dépendance aux secrets CI)
ors:
  api:
    key: dummy-test-key
  profile: driving-car

jwt:
  secret: medhead-poc-jwt-secret-change-me-1234567890
  expiration-minutes: 60
